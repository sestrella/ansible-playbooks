---
- name: Install zim
  git:
    repo: https://github.com/Eriner/zim.git
    dest: ~/.zim
    recursive: yes

- name: Symlink zim file
  file:
    src: "~/.zim/templates/{{ item }}"
    dest: "~/.{{ item }}"
    state: link
  with_items:
    - zimrc
    - zlogin
    - zshrc
